# Puerto del servidor
PORT=3000

# URL de la base de datos PostgreSQL (Railway proporciona esta variable)
DATABASE_URL=postgresql://username:password@host:port/database

# Entorno de ejecución
NODE_ENV=production
